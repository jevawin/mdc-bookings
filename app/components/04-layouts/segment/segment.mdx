import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import * as SegmentStories from './segment.stories.tsx';
 
<Meta of={SegmentStories} />
 
# Segment
 
Reusebale component that uses the section element with the `Container` component used inside the section.

<Canvas of={SegmentStories.Segment} />

<Controls />
