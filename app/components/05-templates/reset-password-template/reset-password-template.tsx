import { ResetPasswordComplete } from './components/complete.tsx';
import { ResetPasswordForm } from './components/form.tsx';
import { ResetPasswordNew } from './components/new-password.tsx';

export const ResetPasswordTemplate = {
	Form: ResetPasswordForm,
	NewPassword: ResetPasswordNew,
	Complete: ResetPasswordComplete,
};
